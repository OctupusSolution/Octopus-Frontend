// Legacy `/reports` route — kept alive as a redirect to the Sales report,
// which is the canonical Pattern-F entry for the Reports module (W2-09).
export { SalesPage as ReportsPage } from "./sales";
