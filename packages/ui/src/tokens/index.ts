// @octopus/ui tokens — public surface of the tokens slice.
// Consumed by packages/config/tailwind/preset.ts and by components directly
// for anything Tailwind's static classes can't express (e.g. gradient stops).

export * from "./colors";
export * from "./typography";
